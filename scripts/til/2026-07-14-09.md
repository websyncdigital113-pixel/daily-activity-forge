# TIL: SQL window functions

_2026-07-14 09:18 UTC_

Window functions (ROW_NUMBER, RANK, LAG, SUM OVER) compute values across a set of rows related to the current row without collapsing them like GROUP BY.
