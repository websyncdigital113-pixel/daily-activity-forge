# TIL: HTTP/2 multiplexing

_2026-07-06 22:15 UTC_

HTTP/2 sends multiple requests over a single TCP connection using streams, eliminating head-of-line blocking at the HTTP layer (though TCP HOL still exists).
