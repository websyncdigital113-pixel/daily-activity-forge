# TIL: Python GIL

_2026-06-10 16:36 UTC_

The Global Interpreter Lock prevents true multi-threading in CPython. Use `multiprocessing` or async I/O instead for CPU-bound or I/O-bound work respectively.
