# TIL: JWT structure

_2026-07-03 16:59 UTC_

A JWT is base64url(header).base64url(payload).signature. The signature is verified server-side; the payload is readable by anyone — don't put secrets in it.
